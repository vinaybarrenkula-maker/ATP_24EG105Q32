tcpip and http 
nodejs is used in backend then browser is not needed
event loop-
stacks and heap are part of js engine 
js support single thread 
127.0.0.1(localhost )

=========================ASSESSMENT====================================
<!-- Create Product REST API with below features
1. Product document structure
        a.productId (required)
        b.productName(required)
        c.price(required, min price 10000 and max price 50000)
        d.brand(required)
        
2. REST API with below operations
        a. Create product
        b. Read all products
        c. Read a product by productId
        d. Update a product by productId
        e. Delete a product by productId -->
==========================StstusCodes=====================================
->200
->201
->301
->400
->401
->
->
->
=================================================================
FindByID()-- used when in find the id {modelname.FindById()}
FindByIdAndUpdate()--used when finding and updation to be done by put()
** before storing the hasing is important that hash code is going to give the jsonwebtoken for it 
**that token going to be used and stored in the data base 
** for security reasons the password is done into jsonwebtokens 


